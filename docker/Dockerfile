FROM alpine:latest

# Install dependencies
RUN apk add --no-cache \
    bash \
    curl \
    git 

# TODO: Install procps for NextFlow

# TODO: Make an alias to get version

# RUN $()
# ENV version = 